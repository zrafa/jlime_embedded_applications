SUBDIRS = libdict-patch ndict babylon demo test dict stardict

EXTRA_DIST = \
	mydict_plugin.h mydict-plugins.spec mydict-ndict.spec \
	libdict-patch/*.c* libdict-patch/include/*.h* libdict-patch/Makefile
